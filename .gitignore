# Scanner exports at the repo root may contain sensitive data — never commit them.
# (testdata/*.csv fixtures are intentionally tracked.)
/*.csv

# Build artifacts
/patchwright
/dist/

# Your real config, scanner exports, and other machine-local data
/local/

bin/

# Dev-only UI tooling (type checker, test harness). Nothing here ships: the page is
# plain ES modules served from the Go binary.
node_modules/
